Bug Fixes:

- Fixed a crash when generating source paths for class path groups.
- [#318] Fixed icons being prepared on the server when creating an error message.
- [#320] Fixed game-versioned mod folders being interpreted as ^version rather than =version if they didn't have a prefix.
